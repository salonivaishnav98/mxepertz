import 'package:flutter/material.dart';

class DirectMessageSc extends StatelessWidget {
  const DirectMessageSc({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
